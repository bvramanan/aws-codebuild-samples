version: 0.2

phases:
  install:
    commands:
      - npm install
      - git init
      - git add --all
      - git config --global user.email "bvramana112@gmail.com"
      - git config --global user.name "bvramanan"
      - git remote add origin https://github.com/bvramanan/aws-codebuild-samples.git
      - git commit -m "test"
      - git push origin master
      - echo 'ls -l'
  build:
    commands:
      - npm test
artifacts:
  files:
    - '**/*'
